# Inertia Systems Handoff Bundle

## 專案背景
這是一個專為「Inertia Systems（慣性系統）」設計的品牌登陸頁。品牌核心理念是「用極致的阻尼，對抗多巴胺的暴政」。視覺風格採用 95% 霧面黑 (Matte Black) 與 5% 代碼冷靜白，結合 Serif (標題) 與 Monospace (內文) 字體，傳達「數位外科醫生」的專業與冷靜感。

## Gemini CLI 任務指示

請嚴格遵守以下開發指令：

1. **先讀完整資料夾結構**：理解現有的前端專案（React + Vite + Tailwind + Framer Motion）。
2. **「只」做後端**：
   - 使用 **local JSON file** (例如 `submissions.json`) 或 **SQLite** 儲存表單資料。
   - **不要**使用外部資料庫。
3. **前端邏輯整合**：
   - 在 `ContactForm.tsx` 組件上「只」增加 `onSubmit` handler、`state` 與 `fetch` 呼叫。
   - 確保連線到您新增的後端 API。
4. **禁止修改視覺與結構**：
   - **不要改任何 markup、樣式、layout、class names**。
   - **保留所有已經做好的前端視覺**（包含 Framer Motion 的滾動阻尼效果）。
5. **最終目標**：
   - 讓 landing page 可以在 localhost 跑起來。
   - 用戶可以從瀏覽器填寫表單並提交。
   - 後端能正確收到並儲存資料。

## 啟動方式建議
- 前端：`npm run dev`
- 後端：建議實作一個簡單的 `server.js` 並與前端整合。

---
*Generated by Gemini CLI on 2026-05-19*
