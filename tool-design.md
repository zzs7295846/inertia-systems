# 引導問題清單（最終版，測試後修正過）

- **Q1**：用一句話告訴我，你今年最想達成的核心商業目標是什麼？
- **Q2**：為了解決這個目標，你目前覺得最痛苦、卡最久的「盲點」或「人工流程」在哪裡？
- **Q3**：如果在接下來的解法中只能二選一，你會優先保證「極致的效率自動化」還是「細膩的使用者體驗」？為什麼？
- **Q4**：在這個流程中，最核心、絕對不能外流或出錯的敏感資料是什麼？（例如：客戶金流、進貨底價）
- **Q5**：專案上線後，你要怎麼定義這個系統是「成功」的？（例如：回本週期、人力釋放比例等）
- **Q6**：如果我們的 AI 導演在沙盒中模擬出顛覆你想像、但成本低一半的新路徑，你有多大的意願放手讓 AI 跑跑看？
- **Q7**：我們不需要你重構現有系統，如果直接用 Agents 在前端即時渲染原型，你最快希望幾天內看到結果？

---

# 骨幹文件 + 字串組合機制

### 骨幹文件：`customer-spec-template.md`
（完整內容見規格書第 6 節）

### placeholder 對應表：
- `{{客戶名}}` ← 擷取自 Q1 的主詞，或使用者登入的基本稱呼
- `{{聯絡方式}}` ← 使用者基本資料（或留空）
- `{{行業}}` ← 由 AI 從 Q1/Q2 的上下文語意中自動辨識填入
- `{{核心目標}}` ← Q1 的回答
- `{{核心痛點}}` ← Q2 的回答
- `{{價值抉擇}}` ← Q3 的回答
- `{{核心資料}}` ← Q4 的回答
- `{{成功指標}}` ← Q5 的回答
- `{{預算}}` ← 從 Q5 成功定義中動態萃取（例如客戶 C 的 6 個月回本期限制）
- `{{時程}}` ← Q7 的回答

### 組合動作：
1. 訪客填完所有問題。
2. 前端 JS 讀取 template。
3. 依對應表替換 placeholder。
4. 產出個人化 markdown。

### 即時回饋（Partial Autonomy 體現）：
填到 Q3 時，前端 UI 會動態 echo 前方 Q1 與 Q2 的答案（例如：「既然您的核心目標是 {{Q1}}，且面臨 {{Q2}} 的痛點，那麼在接下來的解法中...」），讓訪客感覺被深度理解。

---

# 訪客退出 / 跳題機制

- **可以隨時退出嗎**：是
- **跳題會發生什麼**：前端會將該 placeholder 標記為 `PENDING`，後台的 AI 導演 Agents 會在沙盒中針對該模糊區塊生成「對抗性假設（Antagonistic Hypothesis）」，並在最終規格書中特別標註，用以觸發下一次的正面摩擦。
- **骨幹文件對 missing placeholder 的處理**：顯示「（訪客未提供）」

---

# 客戶需求規格書 schema（骨幹章節結構）

1. 客戶基本資訊
2. 核心需求
3. 預算與時程
4. 服務範圍建議

---

# 三次模擬訪談的關鍵 takeaway

- **客戶 A（清楚直接）**：效率重於一切，對 UI 體驗容忍度高。面對此流派，引導工具應極速縮短前期確認，直接切入核心數據（Q4）與痛點（Q2），並利用 Q6 的對抗性問題，測試其對 AI 導演自動化路徑的授權極限。
- **客戶 B（迷茫引導）**：目標模糊、甚至受限於上層壓力。無法直接回答抽象的「二選一」抉擇（此時 Q3 可允許跳過），必須先由工具引導其吐露日常最心累的「人工重複流程（Q2）」，再幫其收斂出具體的「成功指標（Q5）」（如新訓時間減半）。
- **客戶 C（預算有限）**：對防禦性成本高度敏感，排斥大規模系統重構。成功的定義完全與「回本、成本」掛鉤。工具必須在 Q5 引入現實的回收期考量，並透過 Q7 的「不重構、即時渲染原型」卸下其心防，用高性價比的流體路徑切入。
