rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /commissions/{document=**} {
      allow read, write: if true; // For workshop purposes, open access
    }
  }
}
